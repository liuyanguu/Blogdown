# Fragmentation: Saturation  (Built-up Area/Urban Extent) in each ring
# install.packages('raster')
library('raster')
library('rgdal')

setwd("C:/Users/lyhel/OneDrive - nyu.edu/NYU Marron/180716_Draw Circle/Baku")
s1 <- readOGR(dsn = ".",
              layer = "Baku_studyArea")
summary(s1);plot(s1)

crs(s1)
area(s1)
image <- raster("./city_urbFootprint_clp_t3.img")
image2 <- rasterToPoints(image, spatial = F)


